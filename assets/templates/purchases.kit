<!DOCTYPE html>
<html>

<head>
	<link rel="manifest" href="./manifest.json">
    <title>RB TIMED AUCTION</title>
    <link type="text/css" rel="stylesheet" href="assets/css/style.css">

    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="utf-8"> 
</head>

<body class="body USD">
	<div class="js--tal">
        <!-- @include '/assets/components/layout/header' -->
        
        <div class="l-main-container"> 
            <div class="js--nav-pin-waypoint"></div>
            <div class="l-pin-header-wrapper js--pinned-header">
                <div class="l-pin-header-inner">
                    <!-- @include '/assets/components/components/navigation_tabs' -->
                </div>
            </div>
            
            <div class="l-container__medium">
                
                <div v-if="purchasedLots.length > 0" class="h-p-100">
                    <h3 class="t-h3 h-mt-200">This is not an invoice</h3>
                    <div class="o-message__warning h-mb-200">We never send invoices as email attachments. If you are a successful bidder, we will email you a link to a secure website (DocuSign), where your invoice can be viewed. To protect yourself from email fraud, never open unsolicited email attachments.</div>
                    <p>Purchases cannot be removed from the auction site until your invoice is paid in full. Full payment is due within seven days. A storage fee may apply if purchases are not removed by the specified removal date.</p>
                    <hr class="o-hr h-mb-200"/>
                    
                    <div class="o-data-table--header g-wrapper t-h3 t-lh-300 h-color-white h-mb-0">
                        <div class="g-cell t-bold">{{ purchasedLots.length > 1 ? purchasedLots.length + ' Lots ' : '1 Lot ' }}Purchased</div>
                        <div class="">Last updated: <span class="t-bold">1 minute ago</span></div>
                    </div>
                    <div class="o-data-table"> 
                        <div class="o-data-table--body">
                            <div v-for="lot in lotsIveWon" class="o-data-table--row g-wrapper t-lh-300"> 
                                <div class="g-cell"><span class="t-bold">Lot {{ lot.lotNumber }}</span> - {{ lot.description }}</div>
                                <div class="h-ta-right CCY t-bold">750</div>
                            </div>
                        </div>
                    </div>
                    <div class="o-data-table--header h-ta-right t-h3 t-lh-300 h-color-white h-mb-500">
                        Subtotal: <span class="CCY t-bold">1500</span>
                    </div>
                </div>
                <div v-else class="l-container__medium">
                    <div class="h-ta-center h-mt-500 h-mb-500">
                        <img src="assets/img/empty.png">
                        <h3 class="t-h3">You haven't purchased any equipment yet!</h3>
                        <button class="o-button__primary" v-on:click="gotoPage('index')">Start Bidding!</button>
                    </div>
                </div>

            </div>

        </div>
        <footer>Prototype controls: <button v-on:click="emptyPurchases">Show Empty Version</button></footer>
        <!-- @include '/assets/components/layout/footer' -->
        <!-- @include '/assets/components/components/navigation_mobile' -->
        
	</div>
    
    <!-- @include '/assets/components/components/js-includes' --> 

</body>
</html>