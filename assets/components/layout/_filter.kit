<div class="c-search-filter">
   <div class="c-search-filter--filter g-wrapper g--center">
        <div class="g-cell h-hide-mobile" v-if="bidder.number != null">
            <button class="o-button__blank o-button__narrow h-color-white" v-if="creatingChoiceGroup" v-on:click="toggleCreateChoiceGroup">Cancel choice group</button>
            <button class="o-button__blank o-button__narrow h-color-white" v-else v-on:click="toggleCreateChoiceGroup">Create a choice group</button>
        </div>
        <div class="g-cell h-hide-mobile" v-if="bidder.number === null"><!--HOLD SPACE IN VIEWER MODE--></div>
        <div class="g-cell g-mob--full h-ta-center">
            <div class="o-input--toggle-group__inline">
                <label class="o-input--toggle"><input type="radio" name="lotstatus" checked><div>Upcoming Lots</div></label>
                <label class="o-input--toggle"><input type="radio" name="lotstatus"><div>Sold Lots</div></label>
                <label class="o-input--toggle"><input type="radio" name="lotstatus"><div>All Lots</div></label>
            </div>
        </div>
        <div class="g-cell h-hide-mobile h-ta-right">
            <label for="" class="t-field-label h-color-white">Sort by:</label>
            <select class="o-input--select__inline">
                <option value="">Closing Time</option>
                <option value="">Closing Time</option>
                <option value="">Closing Time</option>
                <option value="">Closing Time</option>
            </select>
        </div>
    </div>
</div>